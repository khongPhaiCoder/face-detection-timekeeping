<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head'); %>
  </head>
  <body>
    <header><%- include('../partials/header'); %></header>
    <main>
      <div class="container">
        <h2 style="padding-top: 60px"><%= user.name %></h2>
        <h4>Lương cơ bản <%= user.salaryBase %></h4>
        <% let hours = ((monthlyHour) / 60).toFixed(0) %>
        <h4>
          Tháng này bạn đã làm <% if (hours > 0) { %> <% let minutes
          =(monthlyHour) % 60 %> <%= hours %> giờ <%= minutes %> phút <% } else
          {%> <% let minutes =(monthlyHour) % 60 %> 0 giờ <%= minutes %> phút <%
          } %>
        </h4>
        <h4>
          Tiền lương dự kiến <% if (hours > 160) { %> <%=
          Number(user.salaryBase) + Number(user.salaryBase)/160*1.5*(hours-160)
          %> <% } else {%> <%= Number(user.salaryBase)/160*(hours) %> <% } %>
        </h4>
        <!-- <p><%= user %></p> -->
        <table class="table">
          <thead>
            <tr>
              <!-- <th scope="col">#</th> -->
              <th scope="col">Ngày</th>
              <th scope="col">Trạng thái</th>
              <th scope="col">Thời gian làm việc</th>
              <th scope="col">Xem thêm</th>
            </tr>
            <!-- <tr>
              <td class="input-table">
                <input type="text" placeholder="Type to Filter..." />
              </td>
              <td class="input-table">
                <input type="text" placeholder="Type to Filter..." />
              </td>
              <td class="input-table">
                <input type="text" placeholder="Type to Filter..." />
              </td>
              <td></td>
            </tr> -->
          </thead>

          <tbody>
            <% listHis.forEach(history => { %>
            <tr>
              <!-- <th scope="row">1</th> -->
              <td><%= history.date %></td>
              <td>
                <% if (history.absen =='none') { %>
                <span>Làm ở nhà</span>
                <% } else {%>
                <span>Làm ở công ty</span>
                <% } %>
              </td>
              <td>
                <% if (history.time) { %> <% if (hours > 0) { %> <% let minutes
                =(history.time) % 60 %> <%= hours %> giờ <%= minutes %> phút <%
                } else {%> <% let minutes =(history.time) % 60 %> 0 giờ <%=
                minutes %> phút <% } %> <% } else {%> 0 giờ <% } %>
              </td>
              <td>
                <div class="dropdown">
                  <a
                    class="btn"
                    href="#"
                    role="button"
                    data-bs-toggle="dropdown"
                    aria-expanded="false"
                  >
                    <svg
                      xmlns="http://www.w3.org/2000/svg"
                      width="16"
                      height="16"
                      fill="currentColor"
                      class="bi bi-three-dots"
                      viewBox="0 0 16 16"
                    >
                      <path
                        d="M3 9.5a1.5 1.5 0 1 1 0-3 1.5 1.5 0 0 1 0 3zm5 0a1.5 1.5 0 1 1 0-3 1.5 1.5 0 0 1 0 3zm5 0a1.5 1.5 0 1 1 0-3 1.5 1.5 0 0 1 0 3z"
                      />
                    </svg>
                  </a>

                  <ul class="dropdown-menu">
                    <!-- <li><a class="dropdown-item" onclick="edit_comment('<%= history._id %>')" >Edit</a></li> -->
                    <li>
                      <a
                        href="/timeAnalysis/date/<%= history._id %>"
                        class="dropdown-item"
                        >Chi tiết</a
                      >
                    </li>
                  </ul>
                </div>
              </td>
            </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    </main>

    <!-- <%- include('../partials/footer'); %> -->
  </body>
</html>
